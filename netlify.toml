[build]
  base    = "."
  command = "pnpm install && pnpm --filter @workspace/warriors-fitness run build"
  publish = "dist/public"

[build.environment]
  NODE_VERSION = "20"
  BASE_PATH     = "/"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
